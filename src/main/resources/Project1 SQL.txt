CREATE TABLE Book (
id int GENERATED BY DEFAULT AS IDENTITY UNIQUE PRIMARY KEY,
name varchar,
author varchar,
year_of_create int check (year_of_create > 1900),
person_id int  REFERENCES Person (id));
insert into Book (name, author, year_of_create) values ('The Master and Margarita','Mikhail Bulgakov', '1940');
select * from Book;
ALTER TABLE Book ADD COLUMN taken_at timestamp;
select * from Book;

CREATE TABLE Person (
id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
fullName varchar UNIQUE,
yearOfBirth varchar);
insert into Person (fullName, yearOfBirth) values ('Aliaksandra Lebedzeva Igorevna', '1999');
select * from Person;